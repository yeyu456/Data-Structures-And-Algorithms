package com.algorithms.sort.insertion;

/**
 * Shell Sort Optimized Gap By Pratt, 1971
 * 希尔排序优化间隔 - Pratt, 1971
 */
public class ShellSortPR71 extends InsertionSortR1 {

    /**
     *
     * @param data 待排序数组
     * @return 排序主要语句执行次数
     */
    @Override
    public long sort(int[] data) {
        long count = 0;
        if (!valid(data)) {
            return count;
        }
        //TODO
        return count;
    }
}
